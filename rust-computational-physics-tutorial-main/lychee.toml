# Lychee configuration file
exclude_all_private = true
# Enable caching to check each URL only once
cache = true
# Cache duration: 1 day
max_cache_age = "1d"
# Accept 429 (Too Many Requests) as valid
# The root repository URL is checked once and cached
accept = [200, 429]
# Exclude files matching this pattern
exclude_path = ["print\\.html$"]
# Exclude links matching any of these patterns
exclude = [
    ".*adobe\\.com.*",
    # Exclude GitHub repository sub-paths (edit, commit, blob, tree, etc.)
    # Only the root repository URL will be checked
    "^https://github\\.com/saitama-cond-mat/rust-computational-physics-tutorial/.+",
    # APS journals block automated crawlers with 403
    "journals\\.aps\\.org",
    "link\\.aps\\.org",
    "doi\\.org/10\\.1103/",
    # JPS journals block automated crawlers with 403
    "journals\\.jps\\.jp",
    "doi\\.org/10\\.1143/",
]
